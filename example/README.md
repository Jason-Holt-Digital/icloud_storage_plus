# icloud_storage_plus example

Demonstrates how to use the `icloud_storage_plus` plugin.

## Getting Started

To run this example app, enable the iCloud capability in Xcode. See the plugin
README for detailed instructions. Callback streams such as `onUpdate` and
`onProgress` must attach their listeners synchronously inside the callback.
